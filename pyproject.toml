[project]
name = "astrojones-dev"
version = "0.3.0"
description = "Dev/test tooling for the astrojones-dev plugin (tests the template deploy tools)."
requires-python = ">=3.12"

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
