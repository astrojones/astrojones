# Copy these keys into the GitHub repo secret APP_ENV (multiline key=value).
# The deploy workflow writes them to /opt/nuklaut/secrets/__REPO_NAME__.env.
# Do NOT commit a real .env.
# EXAMPLE_API_KEY=sk-...
